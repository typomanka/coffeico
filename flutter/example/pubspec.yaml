name: coffeico_plus_example
description: Example gallery for the coffeico_plus package.
publish_to: none

environment:
  sdk: ^3.10.9

dependencies:
  coffeico_plus:
    path: ..
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
